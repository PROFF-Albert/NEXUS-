"""Core configuration facade for the production backend."""
from __future__ import annotations

from ..config import (AI_MODE, ACCESS_TOKEN_MINUTES, ALGORITHM, DATABASE_URL, DEMO_EMAIL,
                      DEMO_MASTER, DEMO_PASSWORD, MAX_UPLOAD_MB, OPENAI_API_KEY, OPENAI_MODEL,
                      PBKDF2_ROUNDS, R2_ACCESS_KEY_ID, R2_BUCKET, R2_ENDPOINT_URL,
                      R2_REGION_NAME, R2_SECRET_ACCESS_KEY, SECRET_KEY, STORAGE_QUOTA_GB,
                      VAULT_SESSION_MINUTES)

